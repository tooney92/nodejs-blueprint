const _ = require("lodash");

/**
 * create a new distributor delivery vehicle.
 *
 * @param {Object} deliveryVehicle - a deliveryVehicle object.
 * @returns {Object} - an object.
 */
module.exports.loadashSelecter = async () => {
    
};
